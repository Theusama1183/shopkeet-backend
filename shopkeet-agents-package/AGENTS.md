# AGENTS.md — Shopkeet

Read this file first, every session, before writing any code. It's the entry point for any agent working in this repository — Antigravity, Claude Code, Cursor, or Codex all read `AGENTS.md` the same way.

## What this project is

Shopkeet is a multi-tenant e-commerce SaaS platform for solo/small independent merchants, built on an open-source stack. Full context: `docs/01-mission.md`.

## Source of truth — read in this order before writing code

1. `docs/01-mission.md` — what we're building, for whom, and what's explicitly out of scope
2. `docs/02-tech-stack.md` — the chosen stack and why
3. `docs/03-architecture.md` — system design and deployment topology
4. `docs/04-agent-build-spec.md` — backend build plan, phase by phase (Go API)
5. `docs/05-frontend-agent-spec.md` — frontend build plan and design rules (Next.js)
6. `docs/06-ai-development-rules.md` — how multiple agents coordinate on this codebase
7. `docs/07-expansion-build-spec.md` — backend expansion plan (Phases 8–13: product variants, shipping, discounts, customer accounts, notifications, store settings/tax)
8. `docs/schema.sql` — the full database schema, every table, in one file
9. `docs/api-reference.md` — every REST endpoint, in one place

Always-loaded workspace rules live in `.agents/rules/`. Step-by-step implementation guides live in `.agents/skills/`. Load the relevant skill before starting backend work rather than working from memory of this file alone.

## Non-negotiable constraints (condensed — full reasoning in `docs/04-agent-build-spec.md §0`)

- One Go backend service, internally modular (`tenants`, `media`, `catalog`, `cart`, `orders`, `payments`, `content`). No microservices.
- One Go web framework (Fiber). PostgreSQL only. Redis is cache/queue only — never the only copy of a fact that matters.
- No Kafka, gRPC, API gateway, Kubernetes, service mesh, or GraphQL for the internal API.
- Every tenant-scoped table has `tenant_id` + Row-Level Security from the migration that creates it.
- Payments: Cash on Delivery only for v1. No payment gateway integration.
- Object storage: Cloudflare R2 only, via presigned URLs — not open source, a deliberate flagged exception (see `docs/02-tech-stack.md`).
- Page builder: Puck (`@puckeditor/core`), not a hand-built editor.
- Deployment: **Coolify** on the single VPS = git-push deploys for the API (and, soon, the frontend — no Vercel). Postgres/Redis currently remain manual containers on Coolify's network (re-decided 2026-09-25; was compose+Caddy). Full, enforceable version of these: `.agents/rules/backend-constraints.md`.

## The agent team model

This project is built the way a small software house splits work — full roles and ground rules in `docs/06-ai-development-rules.md`. In short:

- **Architect** owns the docs in this repo and resolves ambiguity — nobody else overrides a documented decision unilaterally.
- **Backend** implements `.agents/skills/backend-build.md`, one phase at a time.
- **Frontend** implements `docs/05-frontend-agent-spec.md` against the contract in `docs/api-reference.md`.
- **QA** verifies each phase's acceptance criteria independently — the agent that built a feature doesn't self-certify it done.

## Build progress (this file is the running log — update after every phase)

- **Phase 0 — Scaffolding: DONE** (verified: `go build ./...` ✓, `go vet ./...` ✓, `go test ./...` ✓, `migrations up: done` against VPS Postgres via SSH tunnel, `/healthz` → `200 {"status":"ok"}`).
  - Stack confirmed locally: Go 1.27 + Fiber + pgx (pgxpool) + Redis + golang-migrate (embedded iofs) + Asynq (background queue, Redis-backed — **not** BullMQ: BullMQ is Node-only, spec `02-tech-stack.md` picks Asynq for the Go backend).
  - VPS `~/infra/docker-compose.yml`: postgres:16-alpine + redis:7-alpine, bound to 127.0.0.1 only, fresh named volumes. Local dev connects via SSH tunnel (`-L 5432 -L 6379`). Old payload.json / shopkeet_apps deleted.
  - No frontend (deliberate per user; CI + compose are backend-only).
- **Phase 1 — Tenants & Auth: DONE** (verified: `go build ./...` ✓, `go vet ./...` ✓, `go test ./...` ✓, `migrations up: done` against VPS Postgres via SSH tunnel, cross-tenant RLS acceptance test passes).
  - Schema in same migration as table: `tenants`, `merchant_users` + `tenant_isolation` policy (`0002_tenants_auth.*`), `FORCE ROW LEVEL SECURITY`.
  - RLS gotcha solved: the Postgres `POSTGRES_USER` (`shopkeet`) is a superuser and bypasses RLS silently. `0003_app_role` adds a dedicated **non-superuser** `shopkeet_app` role that owns the tenant tables; the API and tests connect as `shopkeet_app` (`DATABASE_URL=…shopkeet_app`), not `shopkeet`. `0004_default_grants` auto-grants future phase tables to `shopkeet_app`.
  - Endpoints live: `POST /api/v1/auth/signup`, `POST /api/v1/auth/login` (registered by `auth.RegisterRoutes` in `cmd/api/main.go`). JWT middleware `TenantMW` sets `app.current_tenant` per request.
  - Acceptance test: `TestTenantRLSIsolation` (skips unless `DATABASE_URL` set; run as `shopkeet_app`).
- **Phase 2 — Media Library (Cloudflare R2): DONE** (verified: `go build ./...` ✓, `go vet ./...` ✓, `go test ./...` ✓, `migrations up: done` against VPS Postgres via SSH tunnel, cross-tenant media RLS acceptance test passes).
  - `0005_media_assets.*` — `media_assets` + `tenant_isolation` policy **in the same migration** + `FORCE ROW LEVEL SECURITY`; owner `shopkeet_app` (matches 0003 model).
  - `internal/media/` — `R2` (AWS SDK S3 signing client, endpoint `https://{account_id}.r2.cloudflarestorage.com`) behind the `ObjectStore` interface (fake in tests), `Service` handlers + `RegisterRoutes` mounting under `auth.TenantMW` (RLS-scoped request tx): `POST /media/upload-url`, `POST /media`, `GET /media`, `DELETE /media/:id`.
  - Upload flow (API never sees bytes): client → presigned PUT URL → direct R2 upload → POST /media records metadata. r2_key is `{tenant_id}/{uuid}.{ext}`; a key whose first path segment isn't the caller's tenant is refused 403 — defense in depth on top of RLS.
  - R2 envs: `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`, `R2_PUBLIC_URL` (config.Load requires all-or-none; `/media` routes mount only when configured).
  - Acceptance test: `TestMediaRLSIsolation` (`internal/media/media_test.go`; skips without `DATABASE_URL`, run as `shopkeet_app`) — upload/confirm/list/delete through live HTTP handlers + RLS; asserts cross-tenant list empty, cross-tenant delete 404 (no R2 delete fires), stolen-key confirm 403.
- **Phase 3 — Catalog: DONE** (verified: `go build ./...` ✓, `go vet ./...` ✓, `go test ./...` ✓ against VPS Postgres via SSH tunnel, cross-tenant catalog RLS acceptance test passes).
  - `0006_catalog.*` — `categories`, `products` (with generated `search_vector` + GIN index), `product_categories`, `product_images` — every table gets its `tenant_isolation` policy **in the same migration** + `FORCE ROW LEVEL SECURITY`, owner `shopkeet_app`.
  - Tenant resolution per `docs/03-architecture.md` §2: Next.js middleware resolves the tenant from the hostname and forwards it; storefront endpoints validate `X-Tenant-ID` via `auth.PublicTenantMW` (opens the RLS-scoped request tx, like `TenantMW` but no JWT). `auth.PublicOrAdminMW` serves dual-role routes (`GET /products/:id`): valid Bearer JWT → admin view (any status), otherwise public active-only.
  - `internal/catalog/` — handlers + `RegisterRoutes` on the `/api/v1` router: `GET /products` (public, active-only, `?search=` tsvector + `?category=` slug filters), `GET /products/:id`, `POST/PATCH/DELETE /products/:id` (admin), `POST /products/:id/images` + `DELETE /products/:id/images/:imageId` (admin), `GET /categories` (public). Product JSON embeds ordered `images` (join to `media_assets`) and `categories`.
  - PG gotcha handled: a failing statement (unique violation → 409/400) aborts the request tx, so the middleware's final Commit would 500 ("commit unexpectedly resulted in rollback"). Conflict-prone writes run under a `SAVEPOINT` (`savepoint` helper) so a predicted 4xx keeps the tx usable.
  - Acceptance test: `TestCatalogRLSIsolation` (`internal/catalog/catalog_test.go`; skips without `DATABASE_URL`, run as `shopkeet_app`) — active-only publicity, gallery ordering, search, admin-vs-public view, and full cross-tenant isolation (B cannot list/edit/delete/fetch A's products).
- **Phase 4 — Cart & Inventory: DONE** (verified: `go build ./...` ✓, `go vet ./...` ✓, `go test ./...` ✓ against VPS Postgres via SSH tunnel, cross-tenant cart RLS acceptance test passes; live Redis reservation test passes against VPS Redis).
  - `0007_cart.*` — `carts`, `cart_items` + `tenant_isolation` policy **in the same migration** + `FORCE ROW LEVEL SECURITY`, owner `shopkeet_app`. Adds the invariants the API relies on: `UNIQUE (tenant_id, customer_session)` (one cart per guest session) and `UNIQUE (cart_id, product_id)` (one line per product; adds merge quantity).
  - Guest session resolution: `auth.CustomerMW` resolves the tenant from `X-Tenant-ID` like `PublicTenantMW`, plus the guest `customer_session` from the `shopkeet_session` cookie or `X-Customer-Session` header — minting (and echoing/minting cookie) when absent. Session id rides in `c.Locals("customer_session")`.
  - `internal/cart/` — handlers + `RegisterRoutes` on `/api/v1`: `GET /cart` (returns `{"cart": null}` for a fresh session), `POST /cart` (creates cart on first use, merges same-product quantity, only `status=active` products cartable), `PATCH /cart/items/:id`, `DELETE /cart/items/:id`. Cart JSON embeds product info + line totals + cart total.
  - Reservation (spec Phase 4): best-effort Redis `SETNX shopkeet:reserve:{variant_id}:{unit_index}` (TTL 15min) on add-to-cart via `cart.Reserver` (`RedisReserver` / `NoopReserver` when `REDIS_URL` unset). Variant-scoped since Phase 8. Strictly cache — checkout's `SELECT ... FOR UPDATE` (Phase 5) is the authoritative anti-oversell guard, so add-to-cart never gates on stock (matching the spec's acceptance).
  - New dep: `github.com/redis/go-redis/v9`.
  - Acceptance tests (`internal/cart/`): `TestCartRLSIsolation` — merge/patch/delete + totals, zero-stock product cartable at add time, archived product rejected, cross-session and cross-tenant isolation (B's PATCH on A's line 404s), session minting sets cookie + echoes `X-Customer-Session`; `TestRedisReserver` (skips unless `REDIS_URL` set) — key + TTL verified against live Redis.
- **Phase 5 — Checkout & Orders (COD): DONE** (verified: `go build ./...` ✓, `go vet ./...` ✓, `go test ./...` ✓ against VPS Postgres via SSH tunnel, cross-tenant checkout/orders RLS acceptance test passes).
  - `0008_orders.*` — `orders`, `order_items` + `tenant_isolation` policy **in the same migration** + `FORCE ROW LEVEL SECURITY`, owner `shopkeet_app`. `order_items` embeds `unit_price_cents`/`currency` as a line-item snapshot (price history survives price edits); `orders` holds `total_cents` + `customer_name/phone/email` (phone NOT NULL — the guest's order-verification key).
  - `internal/payments/` — the gateway seam: `Provider` interface + `Registry` (multi-provider methods list). v1 ships only `CODProvider` (name `cod`, creates orders `payment_status=pending`; `paid` is set by fulfillment, see below).
  - `internal/orders/` — `Checkout` (`POST /checkout`, under `auth.CustomerMW`): loads the guest cart, locks every carted product `SELECT ... FOR UPDATE` (the authoritative anti-oversell guard — two concurrent checkouts for the last unit serialize, second gets 409), checks `status=active` + stock inline, runs the COD provider, inserts order + line items (snapshotted prices), decrements `inventory_count`, clears the cart, emits `order.created`, all in one RLS-scoped tx. Also `GetOrder` (`GET /orders/:id?phone=`[+`&email=`] — guest lookup keyed on phone, 404 on mismatch), `ListOrders` (`GET /orders` admin, `?status=` filter), `UpdateStatus` (`PATCH /orders/:id/status` admin; `pending→confirmed→shipped→delivered`, `delivered` sets `payment_status='paid'` + emits `order.paid`, `pending/confirmed→cancelled`; skip-a-step → 400).
  - In-process event bus `internal/platform/events` (`events.Bus/Subscribe/Emit`) surfaces `order.created` / `order.paid`; main.go subscribes a log handler (the seam for future webhooks/Asyncq workers — events are best-effort in process for now).
  - Acceptance test: `TestOrdersRLSIsolation` (`internal/orders/orders_test.go`; skips without `DATABASE_URL`, run as `shopkeet_app`) — two guests race for 3 units (2+2): first checkout 201 (pending/pending/cod, total = 2×p1 + 1×p2, 2 items), inventory decremented exactly once, cart cleared; second guest 409 at checkout (not at add-to-cart time); `order.created` emitted once; lookup 200/404 by phone/email; admin list + status filter; status chain through to delivered→paid with `order.paid` emitted once; tenant B sees zero orders and can't fetch A's order by session+phone.
- **Phase 6 — Content & Page Builder: DONE** (verified: `go build ./...` ✓, `go vet ./...` ✓, `go test ./...` ✓ against VPS Postgres via SSH tunnel, cross-tenant content RLS acceptance test passes). Backend half only — the Puck editor + storefront rendering ship in `apps/web` (frontend agent, not created yet).
  - `0009_content.*` — `posts`, `templates`, `sections`, `redirects` + `tenant_isolation` policy **in the same migration** + `FORCE ROW LEVEL SECURITY`, owner `shopkeet_app`. `layout` (posts/templates/sections) and `placement_rules` (sections) are JSONB storing Puck's data model **verbatim** — the editor's `onPublish` output saves straight into the column, no transformation.
  - **Signup seeds the required-for-v1 chrome** inside the signup transaction: `auth.RegisterTenantCreatedHook` (a `func(ctx, tx, tenantID) error` so auth never imports later phases) → `content.SeedDefaults` creates the home `page` post at `/`, the five templates (`product`, `product_archive`, `cart`, `404`, `order_confirmation`, all `scope='default'`), and `header`/`footer` sections — all `published`. New tenant's storefront works out of the box.
  - `internal/content/` — per `docs/api-reference.md` §Content & Page Builder: public `GET /posts` (one by `post_type`+`route`, or a `post_type` list — published only), `GET /templates/:template_type` (scope=default, published), `GET /sections?section_type=` (published), `GET /redirects/lookup?path=`; admin CRUD for posts, PUT-upsert for templates (Puck save point), sections (incl. popups with `placement_rules`), redirects.
  - Post route change auto-creates a `redirects` row from the old path (spec Phase 6 acceptance). Each rename keeps its own row (`/`→`/home`, then `/home`→`/welcome` = a chain the storefront middleware follows by re-running lookup; built-in `ON CONFLICT (tenant_id, from_path) DO UPDATE` makes same-`from` renames converge).
  - Acceptance test: `TestContentRLSIsolation` (`internal/content/content_test.go`) — uses two `POST /auth/signup` tenants to prove the seed hook; edits home page (title + Puck layout) and verifies the storefront GET reflects it **without redeploy**; route rename → lookup 404 on old, 200 on new + redirect row; drafts invisible publicly and publishable; template/section JSON round-trips through PUT/PATCH; popup lifecycle (draft hidden → published shown); redirect duplicate → 409; tenant B serves only B's own seeded chrome and any admin touch of A's rows 404s.
- **Phase 7 — Observability & Hardening: DONE** (verified: `go build ./...` ✓, `go vet ./...` ✓, `go test ./...` ✓ against VPS Postgres via SSH tunnel; error-shape + /metrics acceptance tests pass). **Last phase — the v1 backend contract is complete.**
  - **Standard error shape**: every error crossing the boundary (handler `*E`, framework `*fiber.Error`, panic recovered by middleware) serializes as `{"error":{"code":"...","message":"..."}}` — no raw stack. `internal/platform/httperr` owns the shape: `New/Status+code/` labeled constructors (`BadRequest/Unauthorized/Forbidden/NotFound/Conflict/Internal`) + `C(status, msg)` for status-derived codes (`invalid_request`, `unauthorized`, `forbidden`, `not_found`, `conflict`, `upstream_error`, `internal_error`) + single Fiber `Handler` wired as `app => fiber.New(fiber.Config{ErrorHandler: httperr.Handler})`. All 92 legacy `c.Status(N).JSON(fiber.Map{"error": ...})` sites across auth/media/catalog/cart/orders/content were migrated; tests now mount the same ErrorHandler so they exercise the contract.
  - **Structured logging**: `internal/platform/observe` — `RequestID` (mints/echoes `X-Request-ID`), `AccessLog` (one structured slog JSON line per request: request_id, method, path, status, latency_ms, tenant_id, role). Mount order in main.go: recover → RequestID → AccessLog → Metrics.
  - **Prometheus /metrics**: `observe.NewMetrics(pool)` registers `shopkeet_http_requests_total{method,route,status}`, `shopkeet_http_request_duration_seconds{method,route}`, and DB-pool gauges `shopkeet_db_pool_{max,acquired,idle,total}` (sampled per scrape). Route label = matched Fiber pattern (bounded cardinality). `/metrics` mounted always; bearer-gated when `METRICS_TOKEN` set (new `config.MetricsToken`). New dep: `prometheus/client_golang` v1.24.
  - **Recover middleware**: `fiber/middleware/recover` with `EnableStackTrace: false` → panic becomes a standard-500 (internal_error, no stack leak).
  - **infra/**: `docker-compose.yml` gains `prometheus` (+`prometheus/prometheus.yml`, scrapes `api:3001/metrics` with the dev token) and `grafana` (provisioned datasource + dashboard provider). The dashboard (`infra/grafana/provisioning/dashboards/shopkeet.json`) renders RPS by route, status distribution, p50/p95/p99 latency, 4xx/5xx rate by route, and the DB pool stats.
  - Acceptance tests (`internal/platform/observability_test.go`): `TestErrorShape` — malformed body → 400 `invalid_request`, handler `*E` → 404 `product_not_found`, panic → 500 `internal_error`, all exact `{"error":{code,message}}` shape; `TestMetricsExposition` — token-gated /metrics returns Prometheus text with `shopkeet_http_requests_total{route="/work",status="200"}`, the latency histogram, and `shopkeet_db_pool_max`, and refuses no-token scrapes.
- **Phase 8 — Product Variants: DONE** (verified: `go build ./...` ✓, `go vet ./...` ✓, `go test -count=1 ./...` ✓ against VPS Postgres as `shopkeet_app` via SSH tunnel + live Redis, `TestVariantsRLSIsolation` cross-tenant variant RLS acceptance passes; migration applied to VPS at version 10/clean).
  - `0010_product_variants.{up,down}.sql` — **apply this migration as the superuser** (`postgres://shopkeet:$`PGPASSWORD): the backfill must DML existing FORCE-RLS rows (`order_items`, `cart_items`), and the app role cannot turn `row_security=off` on FORCE tables (it errors "query would be affected by row-level security"). New tables `product_options`, `product_option_values`, `product_variants` (`sku` nullable, `price_cents`, `inventory_count`, `weight_grams`, `status`, `UNIQUE (tenant_id, sku)`), `product_variant_option_values` (PK `(variant_id, option_value_id)`) — all RLS `tenant_isolation` + FORCE + `OWNER shopkeet_app` in-file. `cart_items`/`order_items` gain NOT NULL `variant_id` (FK→product_variants), historical `order_items.variant_id` backfilled per product, `cart_items` cleared, cart idempotency key moves `UNIQUE(cart_id, product_id)` → `UNIQUE(cart_id, variant_id)`.
  - **Design rule (spec Phase 8): every product always has ≥1 variant.** `CreateProduct` auto-creates one "Default" active variant carrying the flat `price_cents`/`inventory_count` (no option values, `sku` NULL). `products.price_cents`/`inventory_count` become **cached** display values recomputed by the API as `MIN(active variant price)` / `SUM(active variant stock)` after every structural variant change. `UpdateProduct` cascades price/inventory to the sole variant when a product has exactly one.
  - `internal/catalog/{variants.go,routes.go}` — admin `POST /products/:id/options`, `POST /products/:id/variants`, `PATCH /products/:id/variants/:variantId`, `DELETE /products/:id/variants/:variantId` (`DeleteVariant` refuses the last variant→400; FK reference from carts/orders→409; dual code→409). Option-value links validated all-or-nothing and cross-product values→400. Product detail responses embed `product_options[{values}]` + `product_variants[{option_values, sku, price, stock, weight, status}]` via `hydrateDetail`; list responses stay light. `DeleteProduct` cleans up variant/option rows and maps FK violations to 409.
  - Cart/orders now reference variants: `POST /cart` body is `{variant_id, quantity}` (validates product+variant both active), line unique on variant, unit price read from `product_variants.price_cents`; `Checkout` locks `product_variants v JOIN products p ... FOR UPDATE OF v, p`, decrements the variant's `inventory_count`, recomputes cached product totals, and `order_items` (and order/cart JSON) carry `variant_id`. Phase 4 reservation keys are variant-scoped: `shopkeet:reserve:{variant_id}:{unit_index}`.
  - Acceptance tests: `TestVariantsRLSIsolation` (`internal/catalog/variants_test.go`) — auto default variant, option+value creation, variant creation with option links, cache recompute (MIN/SUM) across create/update/delete, cross-product option value→400, duplicate SKU→409, delete-last-variant→400, public storefront exposure, tenant B isolation; Phase 3 `TestCatalogRLSIsolation` and Phase 5 `TestOrdersRLSIsolation` updated to the variant model (multi-variant product acceptance in orders: two variants of p1 purchased independently by two guests). **Bug fixed en route:** `hydrateDetail` originally ran queries nested inside still-open row iterators → pgx "conn busy"; rewritten as one fully-drained query per section.
- **Phase 9 — Shipping: DONE** (verified: `go build ./...` ✓, `go vet ./...` ✓, `go test -count=1 ./...` ✓ against VPS Postgres as `shopkeet_app` via SSH tunnel + live Redis; `TestShippingRLSIsolation` cross-tenant shipping RLS acceptance passes; migration applied to VPS at version 11/clean). **Migration 0011 is pure DDL → runs fine as `shopkeet_app`** (no FORCE-RLS DML, so no superuser like 0010 was needed).
  - `0011_shipping.{up,down}.sql` — `orders` gains `shipping_address_line{1,2}`, `shipping_city`, `shipping_state`, `shipping_postal_code`, `shipping_country`, `shipping_method` (rate-name snapshot), `shipping_cost_cents` (NOT NULL DEFAULT 0); legacy `shipping_address` is made **nullable** (historical only — the API stops writing it). New `shipping_zones` (`name`, `countries TEXT[]`, `regions TEXT[]` — non-empty = region-restricted) + `shipping_rates` (`zone_id`, `name`, `rate_cents`, `free_over_cents`, `sort_order`, idx on zone_id) — all RLS `tenant_isolation` + FORCE + `OWNER shopkeet_app` in-file.
  - `internal/shipping/{shipping.go,routes.go}` — admin `POST/PATCH/DELETE /shipping/zones[/:id]` and `POST/PATCH/DELETE /shipping/rates[/:id]`; public `GET /shipping/rates?country=PK&state=` lists every rate of every zone covering the destination and is what the storefront checkout form consumes. Zone↔rate matching: country ∈ zone `countries` AND (`regions` empty OR state ∈ `regions`); a region-restricted zone **without a state never matches** (no-state resolves to nothing for it). List response adds `state_required: true` when the country has region-restricted zones but no state was given, so the frontend can require the state field (Shopify-like). Checkout resolution returns `ErrStateRequired` in that case (mapped to a 400 asking for `shipping_state`) instead of a silent no-rate. `DeleteZone` with rates → 409; `UpdateZone` changing restricted↔unrestricted with rates → 409; PATCH rate excludes `zone_id`. `zoneMatches` + the pgx `int`/text-array scan helpers handle the `TEXT[]` columns explicitly.
  - **Checkout integration** (`internal/orders/orders.go`): `POST /checkout` now requires structured address (line1 + city + country) and `shipping_rate_id`; a rate that doesn't cover the destination → 400 **before** any inventory mutation; the resolved cost — `rate_cents`, or `0` when `subtotal >= free_over_cents` — is added to `total_cents` and snapshotted with the rate name into the order. Exported `shipping.ResolveRate(ctx, tx, rateID, country, state, subtotalCents) → (*RateQuote, nil)` drives this from the RLS-scoped request tx; nil = no match. `orderRow`/`orderJSON` gained the shipping fields.
  - Acceptance tests: `TestShippingRLSIsolation` (`internal/shipping/shipping_test.go`) — zone/rate CRUD + RLS isolation exactly like the older phases (B can't touch A's rows, public list for B shows only B's rate), region matching (PUNJAB resolves Local, KPK/no-state don't), free-over (`4999 → 550`, `5000 → 0`), unknown-rate and cross-tenant resolves → nil, delete-zone-with-rates → 409, 204 delete statuses; `TestOrdersRLSIsolation` extended in-place — checkout requires the rate, Standard (+500) baked into two orders (2750 / 1700), free-over e2e (6000 subtotal → 0 shipping), mismatched US-only rate for a PK destination → 400 with stock intact.
- **Phase 10 — Discounts: DONE** (verified: `go build ./...` ✓, `go vet ./...` ✓, `go test -count=1 ./...` ✓ against VPS Postgres as `shopkeet_app` via SSH tunnel + live Redis; `TestDiscountsAcceptance` passes; migration applied to VPS at version 12/clean). **Migration 0012 is pure DDL → runs fine as `shopkeet_app`.**
  - `0012_discounts.{up,down}.sql` — `discounts` (`code` normalized to uppercase, `type` percentage|fixed_amount with CHECKs, `value_percent`/`value_cents`/`min_subtotal_cents`/`starts_at`/`ends_at`/`usage_limit`, `times_used` DEFAULT 0, `status` active|disabled, `UNIQUE (tenant_id, code)`) + RLS/FORCE/`OWNER shopkeet_app` in-file; `carts` gains nullable `discount_code`, `orders` gains `discount_code` + `discount_cents NOT NULL DEFAULT 0`.
  - `internal/discounts/{discounts.go,routes.go}` — admin CRUD on `/discounts` under `auth.TenantMW`: `GET/`, `POST/`, `GET/:id`, `PATCH/:id`, `DELETE/:id` (204). `discounts.Resolve(ctx, tx, code, subtotal)` validates without writing (used by the cart path); `discounts.Claim(ctx, tx, code, subtotal)` re-validates and increments `times_used` under `SELECT ... FOR UPDATE` (used by checkout). Errors keep the standard shape: 404 `not_found` for unknown codes, 400 `invalid_request` for inactive/out-of-window/below-min/usage-limit, 409 for duplicate codes.
  - `internal/cart` — `POST /cart/discount` (`{code}`) records the code on the cart after apply-time validation; cart JSON gains `discount_code` + best-effort `discount_cents` (checkout is authoritative).
  - `internal/orders` checkout integration: a cart code is re-validated via `Claim` inside the order transaction before INSERT, so a code that lapsed between apply and checkout is rejected with 400 and the order is not created; `total = subtotal − discount_cents + shipping_cost_cents`, and order JSON carries `discount_code`/`discount_cents`.
  - Acceptance test: `TestDiscountsAcceptance` (`internal/discounts/discounts_test.go`, external package to avoid a discounts↔cart cycle) — percentage apply + 2300 total on a 2000+500 order, expired-at-checkout rejection, usage-limit consumed exactly once (s3a 201, s3b 400), and two racing checkouts for a `usage_limit=1` code — exactly one 201 / one 400 via the `FOR UPDATE` serialization. **Test gotcha fixed en route:** `t.Fatalf` inside a checkout goroutine exited the goroutine before it could send on the result channel → deadlock/timeout; concurrent assertions must use a non-fatal raw request helper collecting statuses.
- **Phase 11 — Customer Accounts: DONE** (verified: `go build ./...` ✓, `go vet ./internal/customers/... ./internal/auth/... ./internal/orders/...` ✓, `go test -count=1 ./...` ✓ against VPS Postgres as `shopkeet_app` via SSH tunnel + live Redis; `TestCustomersRLSIsolation` passes; migration applied to VPS at version 13/clean). **Migration 0013 is pure DDL → runs fine as `shopkeet_app`.**
  - `0013_customers.{up,down}.sql` — `customers` (`email`/`password_hash` nullable, `UNIQUE (tenant_id, email)`, normalized email) + `customer_addresses` (`customer_id` FK, `label`, structured address, `is_default`, index on customer_id) — RLS/FORCE/`OWNER shopkeet_app` in-file; `orders` gains nullable `customer_id UUID REFERENCES customers(id)` so guest checkout stays fully supported (`customer_id` NULL).
  - **Customer JWT scope.** `internal/auth/jwt.go`: `Claims` gains `CustomerID` (`json:"customer_id"`) + `Scope` (`json:"scope"`); `Sign` now sets `scope:"merchant"`, and new `SignCustomer(secret, tenantID, customerID, ttl)` sets `scope:"customer"`. `internal/auth/auth.go`: `TenantMW` and the admin branch of `PublicOrAdminMW` go through a new `parseMerchant`/`parseMerchantWithOptional` pair that **refuse `scope:"customer"` (403)** — so a customer token can never reach an admin route; new `CustomerAuthMW(pool, secret)` requires `scope:"customer"` (refuses merchant tokens 403) and opens the RLS-scoped tx + sets `tenant_id`/`customer_id` locals; new `CustomerOrGuestMW(pool, secret)` — customer JWT path then guest fallback — mounts checkout.
  - `internal/customers/{customers.go,routes.go}` — `Service{pool, secret}`, `New(pool, secret)`; `POST /customers/signup` + `/customers/login` under `auth.PublicTenantMW` (login does bcrypt compare, mints a customer token); `GET /customers/me`, `GET /customers/me/orders` (customer's `orders` via `customer_id`; guest orders invisible), `GET/POST /customers/me/addresses`, `PATCH/DELETE /customers/me/addresses/:id` under `auth.CustomerAuthMW`. Address PATCH is a partial merge; `is_default:true` promotes the address and demotes the others in the same tx. PG gotcha handled: a brand-new address list orders by `is_default DESC, id` (**`customer_addresses` has no `created_at`** — the first draft's `ORDER BY created_at` 500'd and was fixed).
  - Checkout: `internal/orders` checkout extracts `customer_id` from the tx locals (set by `CustomerOrGuestMW`) and inserts it; `orderRow`/`orderJSON` carry `customer_id`.
  - Acceptance test: `TestCustomersRLSIsolation` (`internal/customers/customers_test.go`, external package) — signup mints a token + profile, duplicate-email same tenant → 409, wrong password → 401, me reflects the profile, **guest checkout → order with `customer_id` NULL while the signed-in checkout → linked order visible in `/customers/me/orders`** (admin list confirms exactly one NULL), address lifecycle (create default → second address created non-default → PATCH-promote demotes the first → DELETE + 404), customer-token-on-admin-route → 403, merchant-token-on-customer-route → 403, no-token → 401, and same-email signup on tenant B → distinct customer with zero visible orders.
- **Phase 12 — Notifications: DONE** (verified: `go build ./...` ✓, `go vet ./...` ✓, `go test -count=1 ./...` ✓ against VPS Postgres as `shopkeet_app` via SSH tunnel + live Redis; full suite green; migration applied to VPS at version 14/clean). **Migration 0014 is pure DDL → runs fine as `shopkeet_app`.** Live E2E verified 2026-09-25 via SMTP→Mailpit (welcome + order confirmation + delivered all `status=sent`).
  - `0014_notifications.{up,down}.sql` — `notification_log` (`notification_type` in `order_confirmation|order_shipped|order_delivered|customer_welcome`, `recipient` email/phone, nullable `order_id` FK, `status` sent|failed, `sent_at`) — RLS/FORCE/`OWNER shopkeet_app` in-file; index on `(tenant_id, sent_at)`. Pure DDL like 0011/0012/0013.
  - `internal/notifications/{notifications.go,routes.go,smtp.go}` — `Provider` interface (`Name()`, `Send(ctx, Notification)`) with **three** implementations: `LogProvider` (stdout, default), `ResendProvider` (HTTPS POST to api.resend.com; `RESEND_API_KEY` + `NOTIFICATIONS_FROM_EMAIL`), `SMTPProvider` (stdlib `net/smtp`; **the live production path**). `main.go` provider priority: SMTP (`SMTP_HOST` set) → Resend → Log. SMTP envs: `SMTP_HOST`/`SMTP_PORT`/`SMTP_TLS_MODE` (`starttls`|`tls`|`none`, starttls falls back to plaintext)/`SMTP_TLS_VERIFY`/`NOTIFICATIONS_FROM_EMAIL`. `Service{pool, prov}` subscribes to the internal event bus (`events.Bus`): `order.created` → `order_confirmation`, `order.paid` (emitted at `delivered`) → `order_delivered`, `customers.signup` → `customer_welcome`. Handlers **always return `nil`** so a provider failure never fails the emitting request (checkout/status change); on send error the log row is inserted with `status='failed'` and the error is logged. Notification body uses `tenant.name` (fallback "Shopkeet"), order total/currency, customer name/email/phone. **Shopify-style addressing:** `From: "<StoreName> via Shopkeet <no-reply@<subdomain>.<base>>"`, `Reply-To: support@<subdomain>.<base>` (base from `APP_BASE_DOMAIN`). `GET /notifications/log` (Admin) returns last 100 attempts with status.
  - **Events are emitted post-commit.** Request tx middlewares commit via `auth.commitAndFlush(c, tx, ctx)`, running callbacks registered with `auth.AfterCommit(c, fn)` right after `tx.Commit()`. Emitters (checkout `order.created`, status→delivered `order.paid`, customer signup `customers.signup`) schedule the bus emit there so async handlers (which read on a fresh pool connection) never race the producing transaction. Handlers run with `context.Background()`. **Bug history:** (1) events emitted as typed `fiber.Map` but handlers asserted `map[string]any` → nothing ever fired, fixed to `fiber.Map`; (2) emits inside the open tx raced the commit → `no rows in result set`, fixed with AfterCommit.
  - Wiring: `cmd/api/main.go` builds provider, creates `notifications.New(pool, prov)`, calls `Subscribe(bus)`, mounts `notifications.RegisterRoutes(v1, pool, cfg.JWTSecret)`. `internal/orders` checkout + status update include `tenant_id` in the event payload. `internal/customers` Signup emits `customers.signup` with `tenant_id` + `email` + `customer_id`; `customers.New` takes the bus as a third arg.
  - Acceptance: checkout emits `order.created` → `order_confirmation`; marking order `delivered` emits `order.paid` → `order_delivered`. Live E2E via Mailpit UI proves `sent` rows + actual rendered messages with the per-store From/Reply-To.
- **Phase 13 — Store Settings, Order Notes & Tax: DONE** (verified: `go build ./...` ✓, `go vet ./...` ✓, `go test -count=1 ./...` ✓ against VPS Postgres as `shopkeet_app` via SSH tunnel + live Redis; full suite green; migration applied to VPS at version 15/clean). **Migration 0015 is pure DDL → runs fine as `shopkeet_app`.**
  - `0015_settings.{up,down}.sql` — `tenants` gains `logo_media_asset_id` (FK→media_assets), `default_currency` (DEFAULT 'usd'), `timezone` (DEFAULT 'UTC'), `support_email`, `support_phone`, `tax_rate_percent` (DEFAULT 0). `orders` gains `internal_note` (merchant-only, never shown to customers) and `tax_cents` (NOT NULL DEFAULT 0 — snapshot of tax at checkout). All `ADD COLUMN` only, no RLS changes needed (existing policies cover the new columns).
  - `internal/tenants/{tenants.go}` — `Service{pool}`, `New(pool)`; `GET/PATCH /tenant/settings` under `auth.TenantMW`. Settings are a flat JSON object; PATCH is a partial merge (only provided fields updated). `tax_rate_percent` validated 0–100.
  - `internal/orders` — Checkout fetches `tenants.tax_rate_percent` and computes **tax on the discounted subtotal**: `tax_cents = (subtotal - discount_cents) * rate / 100`; `total = (subtotal - discount) + shipping + tax`; inserts `tax_cents` into the order. (Re-decided 2026-09-25 — tax was previously on the pre-discount subtotal; code review flagged that most jurisdictions tax what the customer actually pays.) `orderRow` gains `taxCents` and `internalNote`; `orderJSON` includes `tax_cents` for all responses, `internal_note` only when `includeInternalNote=true` (admin list + admin status update). New `UpdateNote` handler: `PATCH /orders/:id/note` (Admin) sets/clears the internal note; the note is excluded from customer-facing responses (`GET /orders/:id`, `GET /orders` customer).
  - Acceptance: a tenant with `tax_rate_percent=5` shows correct `tax_cents` on a new order; an internal note set via `PATCH /orders/:id/note` appears in admin `GET /orders` and `GET /orders/:id/status` responses but never in customer-facing `GET /orders/:id?phone=...` or `GET /customers/me/orders`.
- **Deferred:** Redis read-through cache for hot public reads (public catalog + shipping rates) to cut DB load.

## Deployment (VPS + domain)

- **Live API: `https://api.shopkeet.com`** (Cloudflare DNS A-record `api` → `13.61.125.59` on zone `shopkeet.com`, DNS-only; TLS terminated by Coolify's proxy). Full state + handoff checklist: `SHOPKEET-BUILD-DEPLOY.md` and `SHOPKEET-COOLIFY-MIGRATION.md` at the repo root.
- **Coolify is the deploy layer (re-decided 2026-09-25)** — API app uuid `l6modsyezs1vlrv6ly1oqz4i`, source `Theusama1183/shopkeet-backend` `main`, build pack `dockerfile`, `base_directory /apps/api`, port 3001. Git-push deploys via Coolify's auto-deploy webhook; fallback `POST /applications/{uuid}/start?force=true`. Frontend will deploy on Coolify too (no Vercel). This reverses the original compose+Caddy decision — see `03-architecture.md` §7 and `backend-constraints.md`.
- **Hybrid data layer:** `shopkeet-postgres` and `shopkeet-redis` remain manual containers attached to Coolify's Docker network (`postgres` alias collides with `coolify-db` — use `shopkeet-postgres`). Mailpit service = dev/staging email sink; real customer email needs `RESEND_API_KEY` + `NOTIFICATIONS_FROM_EMAIL` (or a real SMTP relay) before merchants ship.
- **Merchant custom domains deferred:** the Caddy on-demand-TLS design was dropped with Caddy; will be re-designed against Coolify's proxy.
- **Login was broken by FORCE RLS in production (500 `internal_error` / `invalid input syntax for type uuid: ""`)** and was untested: the original `LoginHandler` ran a pool-level `tenants JOIN merchant_users` lookup outside any RLS scope. Fix (`internal/auth/auth.go`): resolve `tenants` by subdomain first (tenants is RLS-free by design), then verify credentials on a request transaction with `set_config('app.current_tenant', tenantID, true)` pinned to that tenant. Regression guard: `internal/auth/auth_test.go::TestLoginUnderRLS` (correct login + wrong password/unknown subdomain/unknown email/cross-tenant/empty subdomain all covered). R2 active with custom domain `media.shopkeet.com`.

## Working agreement for any agent in this repo

- Don't start the next phase until the current one's acceptance criteria pass with an automated test, not just a manual check.
- If a task seems to require breaking a non-negotiable constraint, stop and flag it — don't quietly route around it.
- If a change alters an API shape, event name, or schema, update `docs/api-reference.md` or `docs/schema.sql` in the same change, not as a follow-up.
- Nothing on `docs/01-mission.md`'s "explicitly not building" list gets built without being asked, regardless of how natural it seems as a next step.
